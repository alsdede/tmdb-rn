import React from 'react';
import { View, Text } from 'react-native';

const HomeScreen = () => {
    return (
        <View>
            {/* <TopRatedList horizontal movies={movies} />
            <PopularList horizontal movies={movies} />
            <MoviesList horizontal movies={movies} /> */}

            <Text>Home</Text>
        </View>
    );
};

export default HomeScreen;
